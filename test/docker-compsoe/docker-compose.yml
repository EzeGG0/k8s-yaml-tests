version: "3"

services:
  mysql-test:
    build: ../db/
    ports:
      - "3306:3306"
    networks:
      - app-test

  backend-test:
    build: ../backend/
    ports:
      - "8080:8080"
    networks:
      - app-test
    depends_on:
      - mysql-test

networks:
  app-test:
    driver: bridge
